<% content_for :title, "Edit Email Audience" %>

<div class="page-header">
  <h2 class="page-header__title">Edit Email Audience</h2>
</div>

<div class="max-w-600">
  <%= form_with model: @audience, url: email_audience_path(@audience), method: :patch do |f| %>
    <% if @audience.errors.any? %>
      <div class="alert alert-danger error-list">
        <strong class="error-list__title">
          <%= pluralize(@audience.errors.count, "error") %> prohibited this audience from being saved:
        </strong>
        <ul class="error-list__items">
          <% @audience.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="card mb-20">
      <div class="form-group">
        <%= f.label :name, class: "form-label" %>
        <%= f.text_field :name, class: "form-control" %>
      </div>

      <div class="form-group mb-0">
        <%= f.label :email_recipients, "Email recipients (one per line or comma-separated)", class: "form-label" %>
        <%= f.text_area :email_recipients, rows: 6, class: "form-control" %>
      </div>
    </div>

    <div class="form-actions">
      <%= link_to "Back to Email Audiences", email_audiences_path, class: "btn btn-secondary" %>
      <%= f.submit "Update", class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
