<% content_for :title, "Edit Slack Audience" %>

<h1>Edit Slack Audience</h1>

<%= form_with model: @audience, url: slack_audience_path(@audience), method: :patch do |f| %>
  <% if @audience.errors.any? %>
    <div style="color: red; margin-bottom: 12px;">
      <h3><%= pluralize(@audience.errors.count, "error") %> prohibited this audience from being saved:</h3>
      <ul>
        <% @audience.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name, style: "width: 100%;" %>
  </p>

  <p>
    <%= f.label :slack_channel %><br>
    <%= f.text_field :slack_channel, style: "width: 100%;" %>
  </p>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<p>
  <%= link_to "Back to Slack Audiences", slack_audiences_path %>
</p>
